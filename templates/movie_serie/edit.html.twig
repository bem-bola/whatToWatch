{% extends 'base.html.twig' %}

{% block title %}Edit MovieSerie{% endblock %}

{% block body %}
    <h1>Edit MovieSerie</h1>

    {{ include('movie_serie/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('home') }}">back to list</a>

    {{ include('movie_serie/_delete_form.html.twig') }}
{% endblock %}
