<form method="post" action="{{ path('movie_serie_delete', {'id': movie_serie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ movie_serie.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
